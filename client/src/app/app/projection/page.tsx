import type { Metadata } from 'next';
import ProjectionCalculator from '@/views/ProjectionCalculator';

export const metadata: Metadata = { title: 'Projection | DCAlog' };

export default ProjectionCalculator;
